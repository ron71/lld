package com.lld.common.util;

/**
 * marker interface to mark a class which is not following a pattern or having bad design.
 * use it in your class to demonstrate bad design pattern.
 */
public interface IAntiPattern {
}
